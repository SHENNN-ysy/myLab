# 二分查找的几种边界写法整理

## 两种区间定义

闭区间 `[left, right]` 使用 `left <= right`；左闭右开区间 `[left, right)` 使用 `left < right`。循环条件、区间收缩和返回值必须遵循同一种定义。

## 循环不变量

每次收缩区间时都要确保答案仍然位于保留的范围内。找左边界和右边界可以从同一个模板镜像推导。

## 经典应用

旋转排序数组、峰值查找以及具有单调判定条件的“二分答案”问题，都可以从这个不变量出发分析。
