# 动态规划刷题小结：从背包到区间 DP

## 状态定义优先

先明确 `dp[i]` 表示“以 i 结尾”还是“前 i 个”，再推导初始化、转移和答案位置。01 背包倒序遍历容量，完全背包正序遍历容量。

## 区间 DP

区间 DP 通常使用 `dp[l][r]` 表示区间最优解，并按照区间长度从小到大枚举，保证转移需要的子区间已经求出。

## 检查清单

先看数据范围，再定状态含义、转移方程、初始值和遍历顺序。推导不通时优先重新审视状态定义。
