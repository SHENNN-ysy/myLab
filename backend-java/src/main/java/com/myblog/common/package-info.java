/**
 * 公共层：只放各层稳定复用且不包含具体业务流程的类型。
 *
 * <p>包含统一响应包络（result）、全局错误码与角色枚举（enumeration）、
 * 安全常量与鉴权工具（constant、security）、配置绑定（properties）、
 * JSON 序列化约定（json）以及请求上下文（context）。</p>
 */
package com.myblog.common;
